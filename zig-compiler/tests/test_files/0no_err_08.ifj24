const ifj = @import("ifj24.zig");

pub fn main() void {
    var a: i32 = 10;
    var b: i32 = 20;
    var c: i32 = 30;
    var d: i32 = 40;

    var result: i32 = (a + b) * (c - d) / 2;
    
    ifj.write(result); // Expected output: -300

    return;
}